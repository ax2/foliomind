import { readFile } from "node:fs/promises";
const root = new URL("..", import.meta.url);
const load = (file) => readFile(new URL(file, root), "utf8");
const checks = [];
const check = (name, pass, detail) => checks.push({ name, pass, detail });

const [packageJson, cargoToml, tauriConfig, app, errorBoundary, stockWorkspace, marketChart, copilotPanel, marketViews, userState, userStateTransport, nativeUserState, workflow, prd, readme, watchlist, portfolioReview, briefingSchedule, hostIntegrationTest, localHost, developerPanel, styles, marketCalendar, desktopLifecycle, nativeMain, backgroundScheduler, anomalyAttribution, eventReminders, dataStatus, capabilityData, marketStore, executor, watchlistSidebar, cleanGenerated] = await Promise.all([
  load("package.json").then(JSON.parse), load("src-tauri/Cargo.toml"), load("src-tauri/tauri.conf.json").then(JSON.parse),
  load("src/App.jsx"), load("src/components/AppErrorBoundary.jsx"), load("src/components/StockWorkspace.jsx"), load("src/components/MarketChart.jsx"), load("src/components/CopilotPanel.jsx"), load("src/components/SecondaryViews.jsx"), load("src/lib/userStateSchema.js"), load("src/lib/userState.js"), load("src-tauri/src/user_state.rs"), load(".github/workflows/release.yml"), load("docs/prd.md"), load("README.md"), load("src/lib/watchlist.js"), load("src/lib/portfolioReview.js"), load("src/lib/briefingSchedule.js"), load("scripts/local-host.integration.test.mjs"), load("scripts/local-host.mjs"), load("src/components/DeveloperPanel.jsx"), load("src/styles.css"), load("src-tauri/src/market_calendar.rs"), load("src-tauri/src/desktop_lifecycle.rs"), load("src-tauri/src/main.rs"), load("src-tauri/src/background_scheduler.rs"), load("src/lib/anomalyAttribution.js"), load("src/lib/eventReminders.js"), load("src/lib/dataStatus.js"), load("src-tauri/src/capability_data.rs"), load("src/store/useLabStore.js"), load("src-tauri/src/executor.rs"), load("src/components/WatchlistSidebar.jsx"), load("scripts/clean-generated.mjs"),
]);
const quoteFormatting = await load("src/lib/quoteFormatting.js");
const desktopLifecycleClient = await load("src/lib/desktopLifecycle.js");
const monitorLifecycle = await load("src/lib/monitorLifecycle.js");
const monitorConditions = await load("src/lib/monitorConditions.js");
const strategies = await load("src/data/monitorStrategies.js");
const research = await load("src/lib/research.js");
const viteConfig = await load("vite.config.mjs");
const portfolio = await load("src/lib/portfolio.js");
const marketBreadth = await load("src/lib/marketBreadth.js");
const anomalyDetection = await load("src/lib/anomalyDetection.js");
const premarketBriefing = await load("src/lib/premarketBriefing.js");
const liveQuotes = await load("src/components/LiveQuotesStrip.jsx");
const eventCalendar = await load("src/lib/eventCalendar.js");
const piRuntime = await load("src/lib/piRuntime.js");
const localHostClient = await load("src/lib/localHost.js");
const integrations = await load("src/lib/integrations.js");
const settingsView = await load("src/components/SecondaryViews.jsx");
const appTest = await load("src/App.test.jsx");
const userStateClient = await load("src/lib/userState.js");
const labStore = await load("src/store/useLabStore.js");
const qaPlaywright = await load("scripts/qa-playwright.py");
const commandPalette = await load("src/components/CommandPalette.jsx");
const commandPaletteTest = await load("src/components/CommandPalette.test.jsx");
const evidenceDrawer = await load("src/components/EvidenceDrawer.jsx");
const evidenceDrawerTest = await load("src/components/EvidenceDrawer.test.jsx");
const dialogFocus = await load("src/lib/useDialogFocus.js");
const dialogFocusTest = await load("src/lib/useDialogFocus.test.jsx");
const systemNotifications = await load("src/lib/systemNotifications.js");
const refreshPolicy = await load("src/lib/refreshPolicy.js");
const activityRail = await load("src/components/ActivityRail.jsx");
const hostLockTest = await load("scripts/local-host.test.mjs");
const storeTest = await load("src/store/useLabStore.test.js");
const version = packageJson.version;
check("版本号一致", cargoToml.includes(`version = "${version}"`) && tauriConfig.version === version, `当前 ${version}`);
check("真实数据边界", marketViews.includes("DATA_STATES") && marketViews.includes("realDataMode"), "页面必须显式区分未配置、加载、失败和空数据");
check("状态脱敏", userState.includes("normalizeUserState") && userState.includes("revision:") && userState.includes("watchlist:") && userState.includes("briefingSchedule:") && !userState.includes("integration-settings.json") && !userState.includes("apiKey"), "用户状态 schema 只处理脱敏用户事实");
check("发布资产", workflow.includes("SHA256SUMS") && workflow.includes("gh release upload"), "Release workflow 需校验并上传安装包");
check("发布前 Web QA 门禁", workflow.includes("web-qa:") && workflow.includes("scripts/qa-playwright.py") && workflow.includes("needs: [prepare, web-qa]"), "桌面安装包构建前必须通过隔离 Web/Local Host Playwright 回归，并保留失败证据");
check("安装升级路径", tauriConfig.bundle?.windows?.allowDowngrades === false && tauriConfig.bundle?.windows?.nsis?.installMode === "currentUser" && Boolean(tauriConfig.bundle?.windows?.wix?.upgradeCode) && readme.includes("不要求用户先手动卸载"), "Windows 同一产品必须覆盖升级、阻止降级并保留用户配置");
check("阶段设计", prd.includes("Stage 1E") && prd.includes("异动雷达"), "新功能必须先有可验收的 PRD 阶段设计");
check("自选迁移边界", watchlist.includes("parseWatchlistImport") && watchlist.includes("watchlistCsv"), "批量自选导入需先解析校验，导出不得包含实时数据或凭证");
check("复盘证据边界", portfolioReview.includes("createPortfolioReviewSnapshot") && portfolioReview.includes("if (!metrics.pricedCount)") && userState.includes("sanitizePortfolioReviews"), "盘后复盘只能由真实已计价持仓生成，并使用有界脱敏 schema");
check("自动复盘可靠性", app.includes("runDuePortfolioReview") && briefingSchedule.includes("retry-wait") && briefingSchedule.includes("hasFreshPortfolioQuote") && userState.includes("briefingSchedule"), "本地调度必须具备幂等、重试节流、当日真实行情门禁和持久化状态");
check("Local Host 公开契约", packageJson.scripts.test.includes("local-host.integration.test.mjs") && hostIntegrationTest.includes("RUNTIME_BUSY") && hostIntegrationTest.includes("invalid local host session") && hostIntegrationTest.includes("activeRequest"), "真实 HTTP 集成测试必须覆盖鉴权、持久化、并发互斥、取消和状态释放");
check("真实交易日门禁", briefingSchedule.includes("calendar-needed") && nativeUserState.includes("calendar_status") && marketCalendar.includes("REF.EXCHANGE_CALENDAR") && marketCalendar.includes("cn_financial_pro.trade_dates.v1"), "自动复盘必须先通过固定真实交易日历 CAP，失败时禁止猜测交易日");
check("桌面驻留生命周期", cargoToml.includes('features = ["tray-icon"]') && desktopLifecycle.includes("CloseAction::Hide") && desktopLifecycle.includes("foliomind_reconcile") && desktopLifecycle.includes("begin_cleanup") && nativeMain.includes("BackgroundScheduler") && nativeMain.includes("stop_and_join"), "主窗口关闭需隐藏到托盘，显式退出需幂等回收原生调度器、Web Host 与 Pi");
check("原生后台调度", backgroundScheduler.includes("market_calendar::fetch_trading_calendar") && backgroundScheduler.includes("user_state::mutate") && backgroundScheduler.includes("background-review-completed"), "桌面复盘必须由原生 worker 执行真实日历、行情门禁和原子幂等写入");
check("原生调度可观测性", backgroundScheduler.includes("background-scheduler-log") && backgroundScheduler.includes("duration_ms") && backgroundScheduler.includes("cost_from_value") && developerPanel.includes("desktopCostSummary"), "桌面调度调用必须提供脱敏日志、耗时和费用汇总");
check("后台复盘状态与快速退出", backgroundScheduler.includes("background-review-status") && backgroundScheduler.includes("recv_timeout") && app.includes("listenForBackgroundReviewStatus") && prd.includes("Stage 3CX"), "桌面后台复盘失败/等待状态必须回传到已打开界面，退出时应可唤醒并回收调度 worker");
check("用户状态并发控制", userState.includes("revision") && userStateTransport.includes("mergeUserStateChanges") && nativeUserState.includes("save_if_revision") && localHost.includes("USER_STATE_CONFLICT") && hostIntegrationTest.includes("USER_STATE_CONFLICT"), "Web、Local Host 与桌面必须以 revision/CAS 防止后台状态被旧页面覆盖");
check("CAP 能力工作台", developerPanel.includes("调用测试") && developerPanel.includes("参数 Schema") && developerPanel.includes("返回字段") && developerPanel.includes("刷新能力目录") && developerPanel.includes("完整能力目录") && developerPanel.includes("筛选能力") && developerPanel.includes("没有匹配的能力") && developerPanel.includes("discoverCapabilities") && developerPanel.includes("capabilityToolSchema") && developerPanel.includes("复制 Tool Schema") && localHost.includes("BUILTIN_CAPABILITY_CATALOG") && localHost.includes("normalizeDiscoveredCapability") && localHost.includes("/api/dev/capabilities/discover") && styles.includes("min(1320px"), "开发面板必须提供大尺寸能力目录、稳定 schema、真实调用测试、动态目录、检索和 Skill Tool 导出");
check("成本单位隔离", localHost.includes("qverisUnits") && localHost.includes("modelUnits") && developerPanel.includes("多单位"), "CAP 与模型网关费用必须按调用类型保留独立计费单位，未知费用不得估算");
check("自选列视图", marketViews.includes("MARKET_COLUMN_DEFINITIONS") && marketViews.includes("MARKET_COLUMNS_STORAGE_KEY") && marketViews.includes("恢复默认列") && styles.includes("--market-table-columns"), "市场页应支持非敏感、可恢复的自选行情列视图，并保持动态表格对齐");
check("命名行情视图", marketViews.includes("MARKET_VIEWS_STORAGE_KEY") && marketViews.includes("DEFAULT_MARKET_VIEWS") && marketViews.includes("CUSTOM_MARKET_VIEW_ID") && marketViews.includes(">= 10") && marketViews.includes("临时视图"), "命名行情视图只保存列偏好，手动修改进入临时视图并有数量上限");
check("自选市场宽度", marketViews.includes("marketBreadth") && marketViews.includes("自选市场宽度") && prd.includes("Stage 3AG 自选市场宽度概览") && styles.includes("market-breadth-grid"), "市场页应基于真实 liveQuotes 展示上涨/下跌分布和极值，并在窄屏收敛");
check("自选汇总统计", marketBreadth.includes("marketWatchlistSummary") && marketViews.includes("marketWatchlistSummary") && marketViews.includes("自选汇总统计") && styles.includes("market-summary-grid") && prd.includes("Stage 3BN 自选汇总统计"), "市场页应按字段展示当前真实行情的最小值、平均值、中位数和最大值，缺失/过期数据不得补值");
check("异动证据解读", marketViews.includes("AnomalyAttribution") && anomalyAttribution.includes("buildAttributionPrompt") && anomalyAttribution.includes("evidenceIndex") && anomalyAttribution.includes("ANOMALY_ATTRIBUTION_DISCLAIMER") && anomalyAttribution.includes("https?"), "异动解读必须基于真实证据、引用来源、带免责声明并过滤不安全链接");
check("动态自选组盯盘", marketViews.includes("scope === \"watchlist\"") && marketViews.includes("lastSignalBySymbol") && userState.includes("lastSignalBySymbol") && prd.includes("Stage 2E 动态自选组盯盘"), "自选组规则必须动态展开当前标的、逐标的去重并通过状态 schema 脱敏迁移");
check("盯盘规则生命周期", monitorLifecycle.includes("MONITOR_TRIGGER_MODES") && monitorLifecycle.includes("normalizeMonitorExpiresAt") && labStore.includes("isMonitorRuleExpired") && labStore.includes('triggerMode === "once"') && marketViews.includes("盯盘有效期") && nativeUserState.includes("trigger_mode") && nativeUserState.includes("expires_at") && prd.includes("Stage 3CI 盯盘规则生命周期控制"), "盯盘规则必须支持边沿/单次触发和有效期，过期或单次触发后不得继续发起数据请求，并在 Web/桌面状态契约中保留");
check("盯盘真实数据副作用门禁", labStore.includes("runMonitorCheck: async (ruleId)") && labStore.includes("!hasRealDataAccess(runtimeState.integrationStatus)") && storeTest.includes("does not run or persist monitor side effects without a real data connection") && prd.includes("Stage 3CW 盯盘真实数据副作用门禁"), "盯盘 Store 动作必须在真实数据连接边界拒绝，不能把浏览器预览或无凭据结果写入历史与通知");
check("详情页快捷操作", stockWorkspace.includes('aria-label={isWatched ? "取消收藏" : "收藏"}') && stockWorkspace.includes('role="menuitem"') && stockWorkspace.includes("复制证券代码") && prd.includes("Stage 3V 股票详情快捷操作闭环"), "股票详情页收藏与更多菜单必须复用现有状态和可访问操作");
check("图表设置闭环", stockWorkspace.includes('aria-label="图表设置"') && stockWorkspace.includes("显示 MA5") && stockWorkspace.includes("显示网格线") && marketChart.includes("showMovingAverage") && marketChart.includes("showGrid") && marketChart.includes("movingAverage") && prd.includes("Stage 3W 行情图表设置闭环"), "图表设置只能改变显示层，MA5 必须基于真实序列计算");
check("对话快捷指令", copilotPanel.includes('aria-label="快捷指令"') && copilotPanel.includes('role="menuitem"') && copilotPanel.includes("最新行情") && copilotPanel.includes("chooseQuickPrompt") && prd.includes("Stage 3X 对话快捷指令闭环"), "快捷指令只编辑 draft，不绕过发送、取消和运行时互斥协议");
check("渲染故障恢复", app.includes("AppErrorBoundary") && errorBoundary.includes("页面暂时遇到问题") && errorBoundary.includes("重新加载应用") && prd.includes("Stage 3Y 渲染故障恢复"), "组件渲染异常必须显示安全恢复界面，不泄露异常正文或造成白屏");
check("事件双重提醒", eventReminders.includes("EVENT_REMINDER_WINDOW_DAYS") && eventReminders.includes("collectEventReminders") && eventReminders.includes("eventReminderPhase") && marketViews.includes("事件提醒") && prd.includes("Stage 3AB 事件日历双重提醒"), "真实事件需按北京时间提供提前窗口/当天提醒，并以 eventKey + reminderPhase 幂等去重");
check("数据与模型访问解耦", dataStatus.includes("hasRealDataAccess") && dataStatus.includes("hasModelAccess") && app.includes("credentialConfigured) return undefined") && prd.includes("Stage 3AD 数据访问与模型访问解耦"), "CAP 数据页面只需 API Key；对话和 AI 解读仍要求已选模型");
check("桌面固定 CAP 通道", nativeMain.includes("qveris_data_query") && capabilityData.includes("pub fn query") && capabilityData.includes("tools/execute?tool_id=") && capabilityData.includes("connect_timeout") && prd.includes("Stage 3AE 桌面固定 CAP 数据通道"), "桌面 CAP 查询必须走白名单、短超时和统一审计契约");
check("设置连接诊断", marketViews.includes("测试数据连接") && marketViews.includes("queryCapabilityData") && marketViews.includes("connectionTestState") && prd.includes("Stage 3AH 设置页真实数据连接诊断") && readme.includes("测试已保存的数据连接"), "设置页必须提供不依赖模型的真实 CAP 探针，并展示可恢复的来源、时间和耗时");
check("跨链路忙碌状态", copilotPanel.includes("liveDataLoading") && copilotPanel.includes("正在更新行情，完成后可发起分析") && copilotPanel.includes("等待行情更新") && prd.includes("Stage 3AI 跨链路忙碌状态可见性"), "共享资源互斥时对话不能静默拒绝，必须给出可访问的等待状态并保留草稿");
check("CAP 测试结果真实性", developerPanel.includes("capabilityTestOutcome") && developerPanel.includes("可识别的真实行情") && developerPanel.includes("上游没有返回可展示数据") && prd.includes("Stage 3AJ CAP 测试结果真实性"), "能力测试必须区分可用响应、合法空结果和显式失败，不能仅凭 HTTP 2xx 报成功");
check("动态 CAP 参数试验台", developerPanel.includes("测试参数（JSON）") && developerPanel.includes("JSON.parse") && developerPanel.includes("不能是数组或空值") && developerPanel.includes("searchId") && prd.includes("Stage 3AK 动态 CAP 参数试验台") && styles.includes("developer-capability-parameters"), "动态能力必须支持受控 JSON 参数编辑、本地校验和绑定 Search ID 的真实测试");
check("市场表格真实字段映射", marketViews.includes("marketColumnValue(item, quote, column.id)") && prd.includes("Stage 3AL 市场行情表格真实字段映射") && marketViews.includes("我的自选"), "市场自选表格必须将列定义映射为字段 ID，真实行情不得因展示层类型错误全部降级为空值");
check("涨跌方向缺失值中性化", stockWorkspace.includes("changeToneClass(change)") && marketViews.includes("changeToneClass(quote?.change)") && prd.includes("Stage 3AM 涨跌方向缺失值中性化"), "缺失或非法涨跌幅不得默认显示上涨/下跌颜色或进入方向筛选");
check("研究筛选排序", marketViews.includes("sortResearchItems(filtered, liveQuotes, sortKey, sortDirection)") && research.includes("missing values always stay last") && research.includes("RESEARCH_SORT_OPTIONS") && prd.includes("Stage 3AN 研究筛选排序"), "研究页应提供稳定的真实数值排序，并将缺失字段置于末尾");
check("组合分布真实权重", marketViews.includes("portfolioAllocationRows(positions, liveQuotes)") && portfolio.includes("portfolioAllocationRows") && portfolio.includes("row.hasQuote") && prd.includes("Stage 3AO 组合分布真实权重") && styles.includes("allocation-track"), "组合分布只能使用真实市值权重，并明确排除未计价持仓");
check("组合价格正值门禁", portfolio.includes("finitePositiveValue(quote?.price)") && portfolio.includes("number > 0") && prd.includes("Stage 3AQ 组合价格正值门禁"), "组合市值、盈亏和提醒不得消费零值或负数现价");
check("市场表格详情快捷打开", marketViews.includes("market-table-row") && marketViews.includes("openMarketSymbol") && marketViews.includes("selectSymbol(symbol)") && prd.includes("Stage 3AR 市场表格详情快捷打开") && styles.includes("market-table-row:focus-visible"), "市场自选表格标的必须支持鼠标和键盘打开详情，并复用现有选择状态");
check("研究结果详情快捷打开", marketViews.includes("research-row") && marketViews.includes("openResearchSymbol") && marketViews.includes("selectSymbol(symbol)") && prd.includes("Stage 3AS 研究结果详情快捷打开") && styles.includes("research-row:focus-visible"), "研究筛选结果必须支持鼠标和键盘打开详情，并复用现有选择状态");
check("事件卡详情快捷打开", marketViews.includes("event-calendar-footer") && marketViews.includes("openEventSymbol") && marketViews.includes("查看标的") && prd.includes("Stage 3AT 事件卡详情快捷打开") && styles.includes("event-calendar-footer"), "事件日历卡片必须支持进入对应标的详情，并保持来源链接和事件数据边界");
check("全局命令面板", app.includes("<CommandPalette />") && commandPalette.includes("event.ctrlKey") && commandPalette.includes("event.metaKey") && commandPalette.includes("快速搜索") && commandPalette.includes("selectSymbol(entry.id)") && prd.includes("Stage 3AU 全局命令面板") && styles.includes(".command-palette"), "应用必须提供跨平台 Ctrl/Cmd+K 全局搜索，并复用页面与标的选择状态");
check("全局命令面板焦点闭环", commandPalette.includes("returnFocusRef") && commandPalette.includes("trapFocus") && commandPalette.includes("event.key !== \"Tab\"") && commandPalette.includes("aria-modal=\"true\"") && commandPaletteTest.includes("returns focus to the invoking control after closing") && commandPaletteTest.includes("keeps Tab focus inside the dialog") && prd.includes("Stage 3EN 全局命令面板焦点闭环"), "命令面板必须恢复调用入口焦点，并将 Tab 焦点限制在 modal 内");
check("行情证据抽屉焦点闭环", evidenceDrawer.includes("returnFocusRef") && evidenceDrawer.includes("drawerRef") && evidenceDrawer.includes("event.key !== \"Tab\"") && evidenceDrawer.includes("aria-modal=\"true\"") && evidenceDrawerTest.includes("keeps keyboard focus in the drawer and restores the trigger") && prd.includes("Stage 3EO 行情证据抽屉焦点闭环"), "行情证据抽屉必须恢复调用入口焦点，并将 Tab 焦点限制在抽屉内");
check("核心编辑弹窗焦点闭环", dialogFocus.includes("FOCUSABLE_SELECTOR") && dialogFocus.includes("returnFocusRef") && dialogFocus.includes("event.key !== \"Tab\"") && dialogFocus.includes("onCloseRef") && dialogFocusTest.includes("traps Tab, focuses the dialog and restores the trigger") && watchlistSidebar.includes("useDialogFocus") && marketViews.includes("useDialogFocus") && prd.includes("Stage 3EP 核心编辑弹窗焦点闭环"), "自选、组合和盯盘编辑弹窗必须共享焦点恢复、Escape 和 Tab 限制，避免焦点落入被遮挡页面");
check("凭据替换即时刷新", app.includes("integrationStatus?.keyPrefix") && appTest.includes("refreshes the full quote set when the saved credential changes") && prd.includes("Stage 3EQ 凭据替换即时刷新"), "保存或替换 API Key 后必须以非敏感凭据代次触发全量真实行情刷新，避免继续使用旧凭据的缓存或静默等待下一轮轮询");
check("行情 CAP 响应外壳归一化", localHost.includes("function quoteRecord") && localHost.includes('["quote", "quotes", "data", "payload", "result"]') && hostLockTest.includes("normalizes nested quote envelopes") && prd.includes("Stage 3ER 固定行情 CAP 响应外壳归一化"), "固定行情 CAP 必须兼容受限真实响应外壳，同时对缺失或非正价格 fail closed");
check("跨端行情记录提取一致性", quoteFormatting.includes("export function quoteRecords") && quoteFormatting.includes("export function firstQuoteRecord") && labStore.includes("firstQuoteRecord") && labStore.includes("const items = quoteRecords(value)") && settingsView.includes("firstQuoteRecord(payload)") && appTest.includes("last_price") && storeTest.includes("desktop-style nested quote envelopes") && prd.includes("Stage 3ES 跨端行情记录提取一致性"), "Web、桌面、设置连接测试和盯盘必须共享受限行情记录提取规则，缺失 symbol 时仅允许单标的唯一绑定");
check("市场概览卡快捷打开与方向中性", marketViews.includes("index-board-item") && marketViews.includes("changeToneClass(quote.change)") && marketViews.includes("打开${item.name}详情") && prd.includes("Stage 3AV 市场概览卡快捷打开与方向中性") && styles.includes(".index-board-item:focus-visible"), "市场概览卡必须复用标的导航，缺失涨跌幅保持中性并具备键盘焦点");
check("图表序列稳定化", marketChart.includes("function chartTime") && marketChart.includes("function numericOrNaN") && marketChart.includes("const byTime = new Map()") && marketChart.includes("sort((left, right) => left.time - right.time)") && prd.includes("Stage 3AW 行情序列稳定化与图表恢复"), "行情序列交给图表前必须统一时间、过滤无效值、去重并严格升序，异常响应不得导致渲染故障");
check("行情值可信边界", marketStore.includes("function numberOrNull") && marketStore.includes("price <= 0") && marketStore.includes("explicitPercent != null") && prd.includes("Stage 3AX 行情值可信边界"), "行情归一化不得把 null/空字符串价格或涨跌幅转换为 0，非正价格必须被拒绝");
check("自选行情新鲜度", watchlistSidebar.includes("quote-freshness") && quoteFormatting.includes("formatCompactQuoteFreshness") && watchlistSidebar.includes("formatQuoteFreshness") && prd.includes("Stage 3CO 自选行情新鲜度"), "自选侧栏的真实行情必须显式标记新鲜度，过期或未知时间不能看起来像实时数据");
check("多市场行情时区", quoteFormatting.includes("marketTimeZone") && quoteFormatting.includes("America/New_York") && quoteFormatting.includes("Asia/Hong_Kong") && quoteFormatting.includes("formatQuoteTimestamp") && stockWorkspace.includes("stock.market") && liveQuotes.includes("item.market") && marketViews.includes("item.market") && prd.includes("Stage 3CP 多市场行情时区标签"), "跨市场行情时间必须按交易所时区展示，未知市场不得猜测时区，所有主要行情入口需共享该格式化边界");
check("未知市场时区防误判", quoteFormatting.includes("token-bounded") && quoteFormatting.includes("UTC · 时区未知") && stockWorkspace.includes("market={stock.market}") && marketViews.includes("anomaly.market") && marketViews.includes("formatQuoteFreshness(quote.asOf, Date.now(), item.market)") && prd.includes("Stage 3CQ 未知市场时间与证据入口一致性"), "未知市场必须使用明确 UTC 参考，交易所短代码不能被子串误命中，证据/研究/异动入口需传递市场上下文");
check("大规模自选渲染", watchlistSidebar.includes("const WatchlistRow = memo") && watchlistSidebar.includes("normalizedWatchlist") && liveQuotes.includes("const LiveQuoteCard = memo") && marketViews.includes('title={item.name}') && styles.includes("content-visibility: auto") && styles.includes("contain-intrinsic-size") && prd.includes("Stage 3CR 大规模自选渲染与长名称可用性") && prd.includes("Stage 3CS 跨行情入口名称可访问性与大列表回归"), "大规模自选必须复用稳定规范化与记忆化行组件，长列表延迟非可见绘制且所有行情入口保留完整名称");
check("大规模自选完整刷新", storeTest.includes("large watchlist") && storeTest.includes("requestedSymbols") && storeTest.includes("toHaveLength(watchlist.length)") && storeTest.includes("Object.keys(useLabStore.getState().liveQuotes)"), "大规模自选完整刷新必须验证所有请求和真实报价均不静默丢失");
check("数据渠道切换刷新", labStore.includes("previous.settings?.dataChannel !== next?.settings?.dataChannel") && labStore.includes("previous.settings?.dataProvider !== next?.settings?.dataProvider") && app.includes("settings?.dataChannel") && app.includes("settings?.dataProvider") && storeTest.includes("provider or channel changes"), "切换数据渠道或 provider 必须清理旧缓存并触发新的真实数据刷新");
check("过期行情统计门禁", marketBreadth.includes("quoteFreshness") && marketBreadth.includes('freshness.state === "stale"') && marketBreadth.includes("staleCount") && anomalyDetection.includes("freshness.state === \"stale\"") && prd.includes("Stage 3AY 过期行情统计门禁"), "市场宽度和异动雷达不得把已明确过期的行情当作当前信号，过期数量需与缺失数量分开");
check("数据请求取消与资源回收", localHostClient.includes("LOCAL_HOST_ABORTED") && localHostClient.includes("options.signal") && marketStore.includes("abortPendingDataRequests") && marketStore.includes("new AbortController()") && piRuntime.includes("signal") && prd.includes("Stage 3AZ 数据请求取消与资源回收"), "渠道切换时应取消 Local Host 数据请求并保留代次保护，主动取消不得误报超时");
check("Local Host 启动故障可恢复状态", integrations.includes("localHostRequest(\"/api/integration/status\")") && integrations.includes("environment: \"local-host\"") && !integrations.includes("catch { return { credentialConfigured: false, settings: defaultIntegrationSettings, demo: true;"), "集成状态读取必须传播 Host 故障，不能伪装成浏览器预览");
check("Local Host 用户状态单一事实源", userStateClient.includes("isLocalWebRuntime()") && userStateClient.includes("localHostRequest(\"/api/user-state\")") && !userStateClient.includes("Keep browser preview usable when Host is offline") && prd.includes("Stage 3BB Local Host 用户状态单一事实源"), "localhost 用户状态必须以 Host 为唯一事实源，离线不得回退浏览器存储");
check("用户状态加载失败可恢复", labStore.includes("userStateLoading") && labStore.includes("userStateError") && labStore.includes("userStateLoaded: false") && labStore.includes("mergeUserStateChanges(base, local, remote)") && prd.includes("Stage 3BC 用户状态加载失败可恢复与早期编辑保护"), "用户状态失败不得继续触发后台任务，重试和慢速响应需保留可恢复性");
check("搜索标的真实详情闭环", labStore.includes("function dataItemForSymbol") && labStore.includes("dataItemForSymbol(state, symbol)") && stockWorkspace.includes("watchlist.find((item) => item.symbol === symbol) ?? stocks[symbol]") && prd.includes("Stage 3BD 搜索标的详情与真实行情闭环"), "非自选搜索标的详情动作必须仍能发起真实请求，静态目录数据不得冒充行情");
check("共享 CAP 请求独立取消", localHost.includes("subscribeToSharedRequest") && localHost.includes("entry.subscribers") && localHost.includes("entry.controller.abort") && prd.includes("Stage 3BE 共享 CAP 请求的独立取消"), "同一 CAP 请求的单个调用方取消不得中止仍在等待的其它调用方");
check("共享 CAP 缓存与配置切换回收", localHost.includes("cacheSharedResult") && localHost.includes("abortInFlightRequests") && localHost.includes("configuration-changed") && localHost.includes("if (!isAbortError(error))") && prd.includes("Stage 3BF 共享 CAP 结果提交与配置切换回收"), "共享结果必须由上游请求提交缓存，配置切换必须中止旧请求且不伪造错误审计");
check("Host 行情正数边界", localHost.includes('kind === "quote"') && localHost.includes("price <= 0") && localHost.includes("Number.isFinite(price)") && prd.includes("Stage 3BG Host 行情正数边界前移"), "CAP 行情必须在 Host 归一化边界拒绝非有限和非正价格，避免污染缓存与派生指标");
check("CAP 缓存命中语义", localHost.includes("const dataCacheHit = allDataCacheHit(results)") && localHost.includes("cacheHit: dataCacheHit") && localHost.includes("capabilityAuditOperation(cachedResult)") && localHost.includes('return result?.dataCacheHit === true ? "cached-call" : "cap-call"') && prd.includes("Stage 3BH CAP 缓存命中语义与回退门禁"), "固定 CAP 真实直连、短缓存复用和固化工具回退必须使用准确命中语义，避免误触发额外 Search");
check("CAP 回退错误分类", localHost.includes("shouldFallbackToCachedTool") && localHost.includes("if (Number.isFinite(status)) return false") && localHost.includes("shouldFallbackForDataKind(kind, directError)") && prd.includes("Stage 3BI CAP 回退错误分类与成本护栏"), "认证、限流、参数错误、超时和服务端故障不得触发额外 Search；仅能力失效或结构不可用时允许回退固化工具");
check("对话 CAP 回退错误分类", localHost.includes("shouldFallbackForDataKind") && localHost.includes("direct-capability-failed") && localHost.includes("!shouldFallbackForDataKind(input?.kind, error)") && prd.includes("Stage 3BJ 对话 CAP 回退错误分类"), "模型 foliomind_data 与 HTTP 数据路由必须共享回退门禁，避免认证或暂时故障追加 Search");
check("跨市场交易日门禁", marketCalendar.includes("marketcodes_for_positions") && marketCalendar.includes("无法根据") && backgroundScheduler.includes("all_markets_trading") && labStore.includes("marketCodesForPositions") && prd.includes("Stage 3BK 跨市场交易日门禁"), "自动复盘必须按持仓交易所核对真实日历；不支持或无法判定的市场应 fail closed，不得默认为上交所");
check("Local Host 断开回收", localHost.includes("linkAbortSignal") && localHost.includes("clientDisconnectedError") && localHost.includes("status: 499") && hostIntegrationTest.includes("客户端断开") && prd.includes("Stage 3BL Local Host 客户端断开回收"), "客户端断开必须取消所属上游请求、阻止关闭连接后的写入并保留受控诊断，不得继续消耗网络和费用");
check("持久化失败可恢复", labStore.includes('action: error?.code === "USER_STATE_MERGE_CONFLICT" ? "reload" : "retry"') && labStore.includes("retryPersistedUserState") && labStore.includes("throw error") && app.includes("重新读取") && prd.includes("Stage 3BM 持久化失败显式反馈"), "用户状态保存失败必须传播、提供去重恢复入口并区分 CAS 冲突，不得静默报告成功");
check("盯盘 CAP 回退护栏", labStore.includes("shouldBlockMonitorModelFallback") && labStore.includes("directFailures") && labStore.includes("directFailures.some(shouldBlockMonitorModelFallback)") && prd.includes("Stage 3BO 盯盘 CAP 错误分类与回退护栏"), "盯盘固定 CAP 遇认证/限流/超时/服务端失败时不得追加模型 Search，能力缺失才允许受控回退");
check("成本提取可信边界", localHost.includes("function costCandidate") && localHost.includes('"qveris_cost"') && !localHost.includes('"amount"]') && capabilityData.includes('"cost"') && backgroundScheduler.includes('"cost"') && executor.includes('"data"]') && prd.includes("Stage 3BP"), "费用账本只接受显式计费字段，不能把业务 amount/价格/序列值误记为费用，缓存命中不计入上游调用分母");
check("启动连接状态可信化", marketStore.includes("integrationStatusLoading: true") && stockWorkspace.includes('healthState = integrationStatusLoading ? "checking"') && stockWorkspace.includes("不会使用示例行情") && prd.includes("Stage 3BQ"), "连接配置未读取完成前必须保持明确读取态，不能短暂显示预览模式或触发真实行情请求");
check("组合表现趋势", portfolio.includes("portfolioPerformanceSeries") && marketViews.includes("portfolioPerformanceSeries") && marketViews.includes("组合表现趋势") && marketViews.includes("有效盈亏比例不足两次") && prd.includes("Stage 3BR"), "组合趋势只能由有日期和真实盈亏比例的复盘快照派生，缺少两个有效点位时保持空态");
check("行情正数门禁一致性", quoteFormatting.includes("isValidQuotePrice") && stockWorkspace.includes("isValidQuotePrice(quote?.price)") && marketViews.includes("isValidQuotePrice(quoteForSymbol(liveQuotes, item.symbol)?.price)") && prd.includes("Stage 3BS"), "所有行情展示、筛选和自动复盘入口必须只接受有限正数价格，零值和负值保持空态");
check("Local Host 地址安全边界", localHost.includes("validateEndpointUrl") && localHost.includes("validateIntegrationSettings") && localHost.includes("仅允许回环地址使用 HTTP") && hostIntegrationTest.includes("insecureSettings") && prd.includes("Stage 3BT"), "本地 Web Host 必须拒绝不安全远程 HTTP、URL 凭据、查询参数和片段，并与桌面端保持一致");
check("动态 CAP 目录授权边界", localHost.includes("validateDiscoveredCapabilitySelection") && localHost.includes("CAPABILITY_NOT_VERIFIED") && hostIntegrationTest.includes("动态 CAP 测试只允许当前目录已验证的工具") && prd.includes("Stage 3BU"), "动态 CAP 调用测试必须绑定当前已发现的 tool/search，不能由浏览器任意提交未验证工具");
check("Skill 安装状态持久化", userState.includes("installedSkillIds") && userStateTransport.includes("mergeInstalledSkillIds") && labStore.includes("skillItemsForIds") && labStore.includes("toggleSkill: async") && nativeUserState.includes("installed_skill_ids") && prd.includes("Stage 3BV"), "Skill 安装偏好必须跨 Web/桌面 Host、刷新和备份恢复持久化，失败时回滚且不同窗口的独立变更不互相覆盖");
check("固定 CAP 页面回退护栏", labStore.includes("shouldFallbackToAgent") && labStore.includes('"TOOL_CACHE_MISS"') && labStore.includes("if (!shouldFallbackToAgent(error)) throw error") && prd.includes("Stage 3BW 固定 CAP 页面回退错误护栏"), "页面固定 CAP 只有明确能力缺失才可进入 Agent 发现，认证/限流/超时/上游故障不得追加模型调用");
check("动态 CAP 目录会话绑定", localHost.includes("capabilityDirectorySession") && localHost.includes("sessionId: capabilityDirectorySession") && localHost.includes('sessionId = ""') && hostIntegrationTest.includes("afterRestart") && prd.includes("Stage 3BX 动态 CAP 目录会话绑定"), "动态 CAP 目录元数据可保留查看，但调用授权必须绑定当前 Host 会话，重启后需重新发现");
check("详情与图表请求主动取消", labStore.includes("cancel the obsolete") && labStore.includes("upstream call") && labStore.includes("AbortController") && labStore.includes("abortController(detailsRequestController)") && labStore.includes("abortController(seriesRequestController)") && labStore.includes("detailsRequestSymbol") && labStore.includes("seriesRequestContext") && prd.includes("Stage 3CA 详情与图表取消状态回收"), "切换标的或图表周期必须取消旧请求并释放对应 loading 状态，避免无效等待和费用");
check("Skill 备份导出闭环", settingsView.includes("installedSkillIdsForBackup") && settingsView.includes("const installedSkillIds = installedSkillIdsForBackup(skillItems)") && settingsView.includes("installedSkillIds });") && prd.includes("Stage 3BZ Skill 备份导出闭环"), "设置页备份必须保留已安装 Skill ID，并继续通过共享 schema 排除凭据和运行时内容");
check("分层行情轮询", app.includes("LIVE_QUOTE_PRIORITY_REFRESH_INTERVAL_MS") && app.includes("LIVE_QUOTE_FULL_REFRESH_INTERVAL_MS") && app.includes("refreshLiveData({ symbols: prioritySymbols })") && labStore.includes("Array.isArray(options?.symbols)") && labStore.includes("requestedSymbols") && prd.includes("Stage 3CB 分层行情轮询"), "行情轮询必须将重点标的与普通自选分层，重点 15 秒、全量 180 秒，并支持受控 symbol 子集");
check("脱敏开发日志持久化", localHost.includes("developer-logs.ndjson") && localHost.includes("DEVELOPER_LOG_RETENTION_MS") && localHost.includes("MAX_PERSISTED_DEVELOPER_LOGS") && localHost.includes("ensureDeveloperLogsLoaded") && localHost.includes("clearDeveloperLogs") && developerPanel.includes("exportLogs") && prd.includes("Stage 3CC 脱敏开发日志持久化与导出"), "开发面板日志需脱敏持久化、保留期限有界、重启恢复并支持导出/清空");
check("首次运行零副作用盯盘", strategies.includes("defaultMonitorRules = []") && localHost.includes("monitorRules: []") && nativeUserState.includes("monitor_rules: Vec::new()") && userState.includes("LEGACY_SEED_RULES") && nativeUserState.includes("migrate_legacy_seed_rules"), "新安装不得隐式创建可计费盯盘规则；旧未改动种子只能在无活动记录时迁移清空");
check("首次配置清单", stockWorkspace.includes("SetupChecklist") && stockWorkspace.includes("开始使用 FolioMind") && stockWorkspace.includes("不会用演示数据代替") && prd.includes("Stage 3CD 首次运行零副作用与配置清单"), "首次运行应明确连接真实数据、主动获取行情和可选模型配置，不填充演示数值");
check("Playwright 真实数据验收可重复", qaPlaywright.includes("expected_console_errors") && qaPlaywright.includes("for attempt in range(2)") && qaPlaywright.includes("install_button") && qaPlaywright.includes("移动端视口无横向溢出") && qaPlaywright.includes("pageErrors") && prd.includes("Stage 3CE 可重复的真实数据 Playwright 验收"), "本地验收需接受持久化 Skill 状态，并将预期上游错误与前端崩溃分开");
check("盯盘规则管理闭环", marketViews.includes("updateRule") && marketViews.includes("搜索盯盘规则") && marketViews.includes("盯盘规则状态") && marketViews.includes("盯盘规则排序") && labStore.includes("Editing a condition invalidates") && prd.includes("Stage 3CF 盯盘规则管理闭环"), "盯盘规则必须可搜索、筛选、排序和原地编辑；编辑后重置触发边沿并保留审计历史");
check("Web/桌面盯盘状态契约", nativeUserState.includes("pub scope: String") && nativeUserState.includes("last_signal_by_symbol") && nativeUserState.includes("monitor rule scope is invalid") && nativeUserState.includes('rule.scope == "symbol"') && prd.includes("Stage 3CG Web/桌面盯盘状态契约对齐"), "Web、Local Host 与桌面保存盯盘范围和逐标的触发边沿时不得丢字段或误迁移");
check("跨进程用户状态锁", localHost.includes("acquireStateFileLock") && localHost.includes(".user-state.json.lock") && localHost.includes("USER_STATE_BUSY") && nativeUserState.includes("acquire_state_file_lock") && nativeUserState.includes("STATE_LOCK_STALE_AFTER") && nativeUserState.includes("USER_STATE_BUSY") && prd.includes("Stage 3CH Web/桌面跨进程用户状态锁") && hostLockTest.includes("token-owned cross-process user-state lock"), "Web Host 与桌面 Host 共享用户状态文件时必须有一致的跨进程独占锁、有限等待、过期回收和 token 安全释放");
check("本地敏感文件权限", localHost.includes("export async function atomicJson") && localHost.includes("chmod(path, 0o600)") && nativeUserState.includes("PermissionsExt") && nativeUserState.includes("cannot protect {label}") && prd.includes("Stage 3CK 本地敏感文件权限") && readme.includes("0600"), "用户状态、集成配置和工具缓存的原子写入必须在 POSIX 系统强制使用 0600，不能依赖 umask");
check("用户状态损坏恢复", localHost.includes("stateBackupFile") && localHost.includes("readPersistedUserState") && localHost.includes("USER_STATE_CORRUPTED") && localHost.includes("await atomicJson(stateBackupFile, current)") && nativeUserState.includes("fn backup_path") && nativeUserState.includes("fn read_state_file") && nativeUserState.includes("user state recovery") && hostIntegrationTest.includes("last valid backup") && prd.includes("Stage 3CL 用户状态损坏恢复"), "用户状态损坏时必须从最后一次有效快照恢复，双文件不可用则返回可操作错误，不得静默覆盖为默认状态");
check("缺失主状态 fail-closed", localHost.includes("const backupExists") && localHost.includes("缺少主文件，且恢复快照不可用") && localHost.includes("if (!backupExists) return normalizeUserState(defaultState)") && hostIntegrationTest.includes("primary state is missing but its backup is corrupt") && prd.includes("Stage 3CY 缺失主状态文件的安全恢复边界"), "首次空目录可以初始化，但已有损坏备份时不得把缺失主状态静默降级为默认数据");
check("桌面驻留错误可恢复", settingsView.includes("desktopLifecycleError") && settingsView.includes("桌面驻留状态暂时无法读取") && settingsView.includes("checkDesktopLifecycle") && settingsView.includes("desktopLifecycleBusy") && app.includes("reconcileDesktopNow().catch") && app.includes("桌面后台检查暂时失败") && app.includes("listenForBackgroundReviewStatus") && prd.includes("Stage 3CZ 桌面驻留错误可恢复"), "桌面驻留读取、立即检查和后台事件失败必须收敛为可见、脱敏且可重试的 UI 状态，不得留下未处理 Promise");
check("状态变更持久化事务", labStore.includes("toggleRule: async") && labStore.includes("createdNotificationIds") && labStore.includes("createdHistoryIds") && labStore.includes("await get().persistUserState();") && settingsView.includes("markAllRead") && settingsView.includes("notificationError") && prd.includes("Stage 3CM 用户可见状态变更的持久化事务"), "规则、消息、自选和事件提醒的乐观状态必须在保存失败时按记录回滚，事件触达必须晚于持久化");
check("模型网关连接诊断", settingsView.includes("testModelConnection") && settingsView.includes("测试模型") && settingsView.includes("模型连接成功") && localHost.includes("testModelGateway") && localHost.includes('tool_choice: \"none\"') && executor.includes("test_model_connection") && executor.includes('"tool_choice": "none"') && prd.includes("Stage 3CN 模型网关连接诊断"), "配置模型后必须由 Host 直接发起一次无工具模型探针，验证对话链路且不得触发金融能力，并将超时/凭据/网关失败转成可恢复文案");
check("用户状态变更事务完整性", labStore.includes("addRule: async") && labStore.includes("importWatchlistItems") && labStore.includes("savePortfolioPosition") && labStore.includes("replaceUserState") && storeTest.includes("rolls back a newly added watchlist item") && storeTest.includes("rolls back imported watchlist rows") && prd.includes("Stage 3CV 用户状态变更事务完整性"), "所有用户可见状态写入在 canonical Host 保存失败时必须按操作上下文回滚，并保留并发独立编辑");
check("自选顺序可访问调整", watchlistSidebar.includes("moveWatchlistItem") && watchlistSidebar.includes("上移") && watchlistSidebar.includes("下移") && labStore.includes("moveWatchlistItem: async") && storeTest.includes("moves a watchlist item within its group") && prd.includes("Stage 3DA 自选顺序可访问调整"), "自选侧栏在自定义顺序下必须支持键盘可达的同组上移/下移，并通过 canonical Host 持久化");
check("组合持仓搜索与计划筛选", marketViews.includes("portfolioQuery") && marketViews.includes("portfolioPlanFilter") && marketViews.includes("搜索持仓") && marketViews.includes("计划状态筛选") && marketViews.includes("没有符合条件的持仓") && prd.includes("Stage 3DB 组合持仓搜索与计划筛选"), "组合持仓列表必须支持本地搜索、计划状态筛选和可恢复空态，且不改变用户状态或触发额外数据请求");
check("持仓 CSV 导入边界", portfolio.includes("parsePortfolioImport") && portfolio.includes("parseDelimitedLine") && marketViews.includes("导入持仓") && labStore.includes("importPortfolioItems: async") && storeTest.includes("imports portfolio positions atomically") && prd.includes("Stage 3DC 持仓 CSV 导入与安全迁移"), "持仓 CSV 必须先解析校验再通过 canonical Host 一次性保存，忽略运行时行情字段并支持失败回滚");
check("持仓导入预览确认", marketViews.includes("importPreview") && marketViews.includes("确认导入持仓") && marketViews.includes("confirmPortfolioImport") && marketViews.includes("取消不会修改当前组合") && prd.includes("Stage 3DD 持仓导入预览与显式确认") && readme.includes("展示预览，确认后才提交"), "持仓文件选择后只能生成本地预览，显式确认后才可提交 canonical 状态");
check("真实行情研究筛选", research.includes("RESEARCH_FILTER_FIELDS") && research.includes("filterResearchItems") && research.includes("activeResearchFilterCount") && marketViews.includes("research-filter-panel") && marketViews.includes("已保存研究筛选") && marketViews.includes("最多保存 10 个筛选") && prd.includes("Stage 3DE 真实行情研究筛选与条件预设") && readme.includes("真实数值条件"), "研究筛选需支持真实字段数值条件、可复用本地预设和缺失字段安全边界");
check("跨市场路由安全", quoteFormatting.includes("marketRegionFor") && watchlist.includes("marketRegionFor") && briefingSchedule.includes("marketRegionFor") && labStore.includes("marketRegionFor") && labStore.includes("未知市场") && prd.includes("Stage 3DF 跨市场识别与路由安全"), "市场识别必须使用 token 边界；未知市场不得被静默路由为 A 股或错误交易日历");
check("行情技术指标覆盖", marketChart.includes("export function movingAverage") && marketChart.includes("showMovingAverage20") && stockWorkspace.includes("显示 MA20") && marketChart.includes('title: "MA20"') && prd.includes("Stage 3DG 行情趋势指标覆盖"), "行情图表应支持基于真实序列的 MA5/MA20 趋势覆盖，不增加上游请求或绘制合成数据");
check("行情图表交互明细", marketChart.includes("subscribeCrosshairMove") && marketChart.includes("unsubscribeCrosshairMove") && marketChart.includes("market-chart-tooltip") && marketChart.includes("formatQuoteDateTime") && quoteFormatting.includes("export function formatQuoteDateTime") && stockWorkspace.includes('market={stock.market}') && prd.includes("Stage 3DH 行情图表交互明细"), "行情图表十字光标必须显示带市场时区的真实点位明细，并在组件销毁时清理监听");
check("组合历史风险指标", portfolio.includes("export function portfolioRiskReturns") && portfolio.includes("sampleStandardDeviation") && portfolio.includes("correlationFor") && portfolio.includes("weightedVolatility") && portfolio.includes("averageCorrelation") && marketViews.includes("加权波动（样本）") && marketViews.includes("平均相关性") && marketViews.includes("历史数据覆盖") && prd.includes("Stage 3DI 组合历史风险指标"), "组合风险读数必须只基于真实历史序列，明确样本/未年化口径，并在数据不足时保持空值");
check("百分比语义格式", quoteFormatting.includes("signed = true") && marketViews.includes("signed: false") && prd.includes("Stage 3DJ 百分比语义与格式边界"), "方向性变化保留正负号，组合占比与覆盖率等无方向比例必须显式使用无符号格式");
check("百分比语义全入口一致性", marketViews.includes("formatPercent(quote.turnoverRate, { signed: false })") && marketViews.includes("formatPercent(value, { signed: false })") && marketViews.includes("formatPercent(row.weight, { signed: false })") && prd.includes("Stage 3DK 行情与组合比例语义全入口一致性"), "组合明细占比、市场换手率列和汇总必须使用无符号格式，涨跌幅继续保留方向符号");
check("行情刷新主动取消", labStore.includes("cancelLiveDataRefresh: () =>") && stockWorkspace.includes("停止更新") && marketViews.includes("onCancel={cancelLiveDataRefresh}") && prd.includes("Stage 3DL 行情刷新主动取消"), "慢速行情全量刷新必须支持用户主动停止、取消上游请求并阻止过期结果提交");
check("hydration 选择一致性", labStore.includes("const nextWatchlist = hydrated.watchlist.length") && labStore.includes("const selectedSymbol = nextWatchlist.some") && storeTest.includes("anchors the selected workspace to the hydrated watchlist") && prd.includes("Stage 3DM 持久化自选与详情上下文一致性"), "用户状态 hydration 后详情选择必须落在当前规范化自选列表内，保留仍存在的用户选择并安全回退");
check("事件刷新主动取消", labStore.includes("cancelEventsRefresh: () =>") && labStore.includes("eventsRequestGeneration += 1") && marketViews.includes("cancelEventsRefresh") && marketViews.includes("停止更新") && storeTest.includes("cancels a slow event refresh without committing late results") && prd.includes("Stage 3DN 事件刷新主动取消"), "事件日历慢请求必须支持独立主动取消、AbortSignal 和过期结果隔离，不得连带行情请求");
check("事件来源链接安全", labStore.includes('from "../lib/urlSafety.js"') && labStore.includes("url: safeExternalUrl(event.url)") && marketViews.includes("safeExternalUrl(event.url)") && storeTest.includes("keeps event source links on safe web protocols") && prd.includes("Stage 3DO 事件来源链接安全边界"), "事件来源只允许受长度限制的 HTTP(S) 链接，危险协议必须降为空且不影响其它真实事件字段");
check("开发日志嵌套敏感字段脱敏", localHost.includes("SENSITIVE_DEBUG_KEY") && localHost.includes("sanitizeDebugValue") && localHost.includes("export function debugPayload") && hostLockTest.includes("redacts nested credentials from developer payloads") && prd.includes("Stage 3DP 开发日志嵌套敏感字段脱敏"), "动态 CAP 参数和返回摘要必须递归按字段脱敏，不能因 JSON 嵌套或引号格式把凭据写入本地日志");
check("首屏路由按需拆包", app.includes("lazy(() => import(\"./components/SecondaryViews.jsx\")") && app.includes("<Suspense fallback={secondaryViewLoading}>") && viteConfig.includes("manualChunks") && viteConfig.includes("charts") && viteConfig.includes("icons") && prd.includes("Stage 3DQ 首屏路由按需拆包"), "自选首页不得静态加载所有二级工作台，生产构建需保留可缓存的异步路由 chunk 和图表/图标边界");
check("首屏包体积门禁", packageJson.scripts?.build?.includes("check:bundle") && packageJson.scripts?.["check:bundle"]?.includes("check-bundle-size") && prd.includes("控制在 450KB"), "生产构建必须执行首屏入口 450KB 与二级异步 chunk 检查，避免性能回归只停留在人工观察");
check("构建产物清理边界", packageJson.scripts?.clean === "node scripts/clean-generated.mjs" && cleanGenerated.includes('"src-tauri/target"') && cleanGenerated.includes('"src-tauri/target-linux"') && cleanGenerated.includes("Runtime resources such as Pi") && prd.includes("Stage 3DR 构建产物清理"), "构建后只能清理明确列出的生成目录，不得触碰 Pi、portable-git 或用户配置资源");
check("跨入口证券代码匹配", quoteFormatting.includes("export function quoteSymbolKey") && quoteFormatting.includes("export function quoteForSymbol") && portfolio.includes("quoteForSymbol(liveQuotes") && labStore.includes("quoteForSymbol(quotes") && prd.includes("Stage 3DS 跨入口证券代码匹配"), "行情、持仓、提醒和详情跳转需要在保留原始代码的同时兼容 provider 交易所后缀，避免真实报价无法进入各入口");
check("跨入口行情解析一致性", quoteFormatting.includes("export function quoteForSymbol") && portfolio.includes("quoteForSymbol(liveQuotes") && briefingSchedule.includes("quoteForSymbol(liveQuotes") && marketBreadth.includes("quoteForSymbol(liveQuotes") && research.includes("quoteForSymbol(quotes") && watchlist.includes("quoteForSymbol(quotes") && liveQuotes.includes("quoteForSymbol(liveQuotes") && marketViews.includes("quoteForSymbol(liveQuotes") && anomalyDetection.includes("quoteForSymbol(liveQuotes") && labStore.includes("quoteForSymbol(quotes") && prd.includes("Stage 3DT 跨入口行情解析一致性"), "所有行情消费入口必须共享安全证券代码解析，唯一后缀匹配才可回填，歧义时保持空态");
check("事件日历跨市场一致性", eventCalendar.includes('EVENT_TIME_ZONE = "Asia/Shanghai"') && eventCalendar.includes("dateKeyFromTimestamp") && marketViews.includes("quoteSymbolKey") && marketViews.includes('timeZone: "Asia/Shanghai"') && prd.includes("Stage 3DU 事件日历跨市场一致性"), "事件日期必须按北京时间归一化，事件与持仓/自选关联必须复用统一证券代码 key，避免跨市场错日或漏匹配");
check("组合复盘事件窗口一致性", portfolioReview.includes('import { eventDateKey } from "./eventCalendar.js"') && portfolioReview.includes("quoteSymbolKey(symbol)") && portfolioReview.includes("const endKey") && portfolioReview.includes("eventKey < startKey") && prd.includes("Stage 3DV 组合复盘事件窗口一致性"), "组合复盘的事件关联必须复用北京时间日期 key 和规范化证券代码，并以本地日历窗口筛选，避免跨时区错日、交易所后缀漏匹配或毫秒窗口漂移");
check("盘前真实数据摘要", premarketBriefing.includes("buildPremarketBriefing") && premarketBriefing.includes("normalizePremarketNews") && premarketBriefing.includes("normalizePremarketMarketNews") && labStore.includes("generatePremarketBriefing") && labStore.includes("kind: \"sentiment\"") && labStore.includes("kind: \"market_news\"") && labStore.includes("kind: \"index_levels\"") && labStore.includes("kind: \"commodity\"") && marketViews.includes("盘前数据摘要") && marketViews.includes("停止更新") && prd.includes("Stage 3DX 盘前上下文 CAP"), "盘前摘要必须按持仓并行聚合已验证的新闻、行业/宏观、指数和商品 CAP，未返回结果保持空态，并支持取消、重试和来源追溯");
check("跨端稳定 CAP Agent 工具", nativeMain.includes("foliomind_data") && executor.includes('operation == "data"') && executor.includes("valid_data_input") && capabilityData.includes("CapabilityQueryInput") && capabilityData.includes("pub fn query") && localHost.includes("foliomind_data") && localHost.includes("BUILTIN_CAPABILITY_CATALOG") && prd.includes("Stage 3DY 跨端稳定 CAP Agent 工具"), "Web 与桌面 Agent 必须共享固定白名单 CAP 工具，优先直连真实能力并保留审计、取消和空数据边界");
check("桌面 CAP 缓存与失效", capabilityData.includes("cache_ttl") && capabilityData.includes("MAX_CACHE_ENTRIES") && capabilityData.includes("cap-cache-hit") && capabilityData.includes("pub fn clear_cache") && nativeMain.includes("capability_data::clear_cache()") && prd.includes("Stage 3DZ 桌面 CAP 缓存与成本边界"), "桌面固定 CAP 查询必须使用有界短 TTL 缓存，空/失败结果不得缓存，凭证或端点变更后必须失效并写入缓存命中审计");
check("Local Host CAP 缓存容量与回收", localHost.includes("MAX_DIRECT_DATA_CACHE_ENTRIES = 256") && localHost.includes("cache.delete(key)") && localHost.includes("while (cache.size > limit)") && localHost.includes("if (cached) directDataCache.delete(cacheKeyValue)") && hostLockTest.includes("keeps the direct CAP cache bounded") && prd.includes("Stage 3EA Local Host CAP 缓存容量与长驻回收"), "Local Host direct CAP 缓存必须有界、命中续期并清理过期条目，长驻调试不能无限增长或复用过期真实数据");
check("Local Host 交易日历实时门禁", localHost.includes("trading_calendar: 0") && capabilityData.includes('cache_ttl("trading_calendar").is_zero()') && hostIntegrationTest.includes("never caches trading-calendar gates") && prd.includes("Stage 3EB 交易日历实时门禁"), "交易日历不得使用 Local Host 或桌面缓存，自动复盘必须基于当前真实交易日历判断");
check("系统通知分级与站内留痕", systemNotifications.includes("SYSTEM_NOTIFICATION_MODES") && systemNotifications.includes("shouldSendSystemNotification") && systemNotifications.includes("systemNotificationMode()") && systemNotifications.includes('notification?.severity === "critical"') && settingsView.includes("系统通知级别") && prd.includes("Stage 3EC 系统通知分级策略"), "系统通知可按关键级别过滤，但站内消息、审计和费用不受影响");
check("行情自动刷新策略", refreshPolicy.includes("REFRESH_POLICIES") && refreshPolicy.includes("saveRefreshPolicy") && refreshPolicy.includes("subscribeRefreshPolicy") && app.includes("refreshPolicyConfig(refreshPolicy)") && app.includes('policy.id === "manual"') && settingsView.includes("行情自动刷新策略") && settingsView.includes("频繁刷新会增加上游调用次数和费用") && prd.includes("Stage 3ED 行情自动刷新策略"), "行情轮询应支持实时、均衡和手动策略，手动模式停止后台请求且不改变显式刷新与真实数据门禁");
check("桌面 CAP 瞬时故障重试", capabilityData.includes("MAX_RETRY_ATTEMPTS") && capabilityData.includes("retryable_status") && capabilityData.includes("retry_delay") && capabilityData.includes("RETRY_AFTER") && capabilityData.includes("error.is_timeout()") && prd.includes("Stage 3EF 桌面 CAP 瞬时故障重试"), "桌面 CAP 通道需对连接/超时和限流/网关瞬时故障进行一次有界重试，尊重 Retry-After 且不把永久错误重复发送");
check("跨端盘前摘要自动调度", briefingSchedule.includes("premarketSlot") && briefingSchedule.includes("premarketEnabled") && briefingSchedule.includes("premarketLastSuccessKey") && labStore.includes("runDuePremarketBriefing") && labStore.includes("premarketBriefingScheduleBusy") && userState.includes("premarketBriefing") && userState.includes("sanitizePremarketBriefing") && backgroundScheduler.includes("PREMARKET_EVENT") && backgroundScheduler.includes("premarket_due_state") && desktopLifecycleClient.includes("listenForBackgroundPremarket") && app.includes("listenForBackgroundPremarket") && marketViews.includes("自动生成盘前摘要") && prd.includes("Stage 3EG 跨端盘前摘要自动调度"), "盘前摘要必须在 Web/桌面共享北京时间、交易日历、真实 CAP、持久化、通知和幂等重试边界");
check("盯盘条件策略语义", ["price_change", "volume_spike", "technical", "core_event", "capital_flow", "sentiment"].every((id) => monitorConditions.includes(`id: "${id}"`)) && strategies.includes('id: "technical"') && strategies.includes('id: "core_event"') && strategies.includes('id: "capital_flow"') && strategies.includes('id: "sentiment"') && prd.includes("Stage 3EH 盯盘条件与运行策略语义对齐"), "六类盯盘条件必须在编辑器、真实数据字段和运行策略中保持同一语义，并兼容旧 news_risk 规则");
check("CAP 测试主动取消", developerPanel.includes("停止测试") && developerPanel.includes("LOCAL_HOST_ABORTED") && localHostClient.includes("options.signal") && integrations.includes("marketcode = \"212001\", options = {}") && integrations.includes("{ ...options, method") && prd.includes("Stage 3EI CAP 调用测试主动取消"), "开发面板的长耗时能力测试必须可主动取消，取消后不得提交迟到结果或误报失败");

check("CAP 目录加载主动取消", developerPanel.includes("停止加载") && developerPanel.includes("directoryController") && developerPanel.includes("directoryGeneration") && developerPanel.includes("signal: controller.signal") && prd.includes("Stage 3EJ 能力目录加载主动取消与重试"), "动态能力目录 Search 必须支持本地停止、重试和迟到响应隔离，不得阻塞本地调试或覆盖已有目录");
check("自选快速筛选", watchlistSidebar.includes("搜索自选") && watchlistSidebar.includes("filterQuery") && watchlistSidebar.includes("清除自选搜索") && watchlistSidebar.includes("category") && watchlistSidebar.includes("market") && prd.includes("Stage 3EK 自选快速筛选"), "自选侧栏必须支持本地名称/代码/分类/市场/分组筛选，显示命中数量并提供可访问清除，不触发网络请求或误导性重排");
check("导航未读提示可访问性", activityRail.includes("条未读") && activityRail.includes('aria-hidden="true"') && prd.includes("Stage 3EL 导航未读提示可访问性"), "消息导航必须提供稳定的未读语义名称，视觉徽标不得被读屏重复朗读");
check("站内消息交互语义隔离", settingsView.includes("function NotificationCard") && settingsView.includes('className="notification-main"') && settingsView.includes('role="button"') && settingsView.includes("notification-item-actions") && appTest.includes("keeps notification actions outside the interactive message surface") && prd.includes("Stage 3EM 站内消息交互语义隔离"), "消息卡片容器不得承载 role=button 并嵌套子按钮，整卡阅读与查看标的/盯盘操作必须保持独立焦点和事件边界");

const failed = checks.filter((item) => !item.pass);
console.log(JSON.stringify({ version, reviewedAt: new Date().toISOString(), checks, result: failed.length ? "needs-attention" : "pass" }, null, 2));
if (failed.length) process.exitCode = 1;
