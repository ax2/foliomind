# 上市验收状态

更新时间：2026-09-12；当前正式版本：0.1.235（上一正式版：0.1.234）；待审候选：PR #88。

本文件记录验收证据与缺口，不以版本数量或静态架构检查代替实际验收。

## 当前版本

0.1.235 在 0.1.234 基础上增加启动工作区恢复门禁：Local Web/桌面 Host 状态尚未完成读取前不渲染默认自选行，避免初始化阶段把内置元数据误认为用户数据；并保留桌面 Host 集成状态对账、可复用工作区视图、凭据对账、错误脱敏、Release 远端资产校验、窄屏行情和离线提示。消息不携带凭据或配置。正式发布已由 GitHub Actions run `34017177415` 完成：Web QA、全量测试、架构审查、安全审计及 Windows MSI/NSIS、macOS DMG 构建全部成功；正式 Release `v0.1.235` 为非草稿、非预发布，包含 3 个安装包和 `SHA256SUMS.txt`，远端下载复核通过。构建后的生成目录按白名单清理，工作区保持干净。待审候选 PR #88 又补充了 Local Host 乱序状态保护、外部同前缀凭据替换检测，以及 Web/桌面统一的固定长度 SHA-256 `credentialRevision`；本轮候选需在提交后由 CI 重新验证，尚未合入正式版本。

## 必须继续验证的验收项

| 项目 | 当前证据 | 结论与下一步 |
| --- | --- | --- |
| 真实行情与对话 | 单元/Host 集成测试覆盖真实数据边界；固定 CAP 与模型探针已实现 | 自动化大多使用受控响应；需要用获授权的真实凭据，在安装后的 Windows/macOS 记录行情、详情、事件、对话的成功率和延迟，尚未证明上市可用性 |
| 凭据轮换 | 同前缀、失败保存、迟到响应、同源双标签页、Local Host 外部同前缀文件替换，以及 Web/桌面同一 Key 的固定长度 SHA-256 revision 已验证 | 同源通知已覆盖；桌面 keyring 的外部修改、跨源通知和进程休眠后的恢复仍需在对应平台实测 |
| 安装升级 | workflow 构建 DMG/EXE/MSI，配置包含稳定升级标识 | 构建通过不等于真实覆盖升级成功；需要干净系统上旧版安装→新版升级→配置/用户数据保留→卸载的验收记录 |
| 安装包信任 | 当前发布 workflow 未配置平台签名/公证步骤 | 签名身份和证书可用后接入，并在干净系统验证下载、启动提示与签名有效性 |
| 长期运行与费用 | 现有测试覆盖短缓存、并发、取消、费用提取、调度幂等 | 仍需跨交易时段的长期运行、断网恢复、休眠恢复测试，以及内存/请求次数/实际费用记录 |
| CI 质量门禁 | Release/桌面 CI 执行全量测试、架构审查和安全审计；Release 还会重新下载并校验资产 SHA-256；本地使用官方 npm registry 复核生产依赖，当前 0 个高危及以上漏洞；发布 run `34017177415` 的 Web QA、测试与三平台桌面 job 全部成功 | 新版本必须保持失败关闭，不得以审计或架构检查警告放行 |
| 发布交付 | v0.1.235 已由 CI 正式发布，包含 DMG/EXE/MSI/SHA256SUMS；Release 非草稿、非预发布，4 个资产远端校验通过 | 后续版本继续核对正式 Release、三类安装包及校验和 |

## 后续实施顺序

1. 在 Windows/macOS keyring 和休眠恢复环境验证统一的 SHA-256 凭据版本标识、缓存隔离和窗口重新可见对账；Local Host 文件型凭据的外部替换检测已由 PR #88 覆盖。
2. 建立真实数据跨端验收记录，分别记录冷/热缓存、部分失败、重试与费用；由实测确定性能目标。
3. 在 Windows/macOS 干净环境完成安装与升级测试，接入已有可用签名身份。
4. 做长期运行和休眠/断网恢复验收，再据证据判断是否满足上市要求。

这些缺口不应由无限增加局部 UI 功能来替代。当前仍属于验证中的迭代版本。
