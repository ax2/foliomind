# FolioMind 架构与整体实现 Review

这是发布前和周期性质量门禁，不替代代码 Review。当前仓库提供 `npm run review:architecture` 做可重复的静态检查，检查结果只描述“是否需要关注”，不自动修改代码。

## 频率与触发

- 每个正式 Release 前运行一次，与 `npm test`、`npm run build`、安全审计和桌面端构建一起记录。
- 快速迭代期间每两周人工 Review 一次；涉及数据渠道、凭据、状态 schema、Tauri Host 或安装升级逻辑时立即追加一次专项 Review。
- 当连续两个版本出现同一类失败、数据延迟或用户无法恢复时，必须升级为重构候选项，而不是继续增加补丁。

## 人工检查清单

1. 数据边界：页面是否只消费真实 CAP 返回；缺失、失败、过期和部分数据是否分别呈现；是否存在示例数据或由其它字段推算出的数值。
2. 性能与成本：是否复用固化 CAP 工具和短时缓存；并发、超时和重试是否有上限；CAP 与模型网关费用是否在开发面板可追溯。
3. 安全与隐私：API Key、模型配置、提示词和原始响应是否保持在 Host/运行时边界内；日志、备份和错误信息是否脱敏。
4. Web/桌面一致性：状态 schema、错误恢复、取消、通知和开发面板是否共用同一语义；桌面升级是否覆盖安装而不要求先卸载。
5. 可维护性：跨页面逻辑是否已提取为纯函数；重复的 provider 适配、状态归一化和数据状态文案是否需要合并；测试是否覆盖失败和空态。
6. 发布可靠性：版本号、PRD 阶段、Release tag、安装包、校验和及升级路径是否一致；失败时能否从日志定位到接口、参数、返回摘要和原因。

Review 结论应记录在对应 PR 或 Release notes 中，至少包含“继续演进 / 优先重构 / 暂停发布”之一和下一阶段验收条件。

## 最近一次专项 Review（2026-09-02 · Stage 3BV）

结论：继续演进。

- 发现 Skill 市场的安装状态只存在内存，刷新 Web 或重启桌面会静默恢复为默认值；现在只保存非敏感的 `installedSkillIds`，Web、Local Host 与桌面 Rust Host 使用同一字段。
- Skill 切换等待 canonical 用户状态保存成功，失败时回滚 UI 并提供重试通知；旧状态迁移到两个内置 Skill，备份不会包含 Skill 内容、凭据或运行日志，CAS 合并按 Skill ID 处理。
- 下一阶段继续补充真实桌面跨重启、备份恢复和保存失败回归，并在平台签名密钥可用后完成安装包干净安装、覆盖升级和卸载恢复烟测。

## 最近一次专项 Review（2026-09-02 · Stage 3BW）

结论：继续演进。

- 发现页面固定 CAP 请求在任意异常下都会转入 Agent 发现路径，认证失败、限流、超时和上游故障可能追加模型调用与费用。
- 现在页面层只接受明确的 `TOOL_CACHE_MISS`、`CAPABILITY_NOT_FOUND` 或 HTTP 404 能力缺失信号进入发现；其它错误沿用直接 CAP 的友好错误与重试状态。
- 缺少结构化错误码时默认不回退，仅识别明确的能力缺失文案；该防御层与 Local Host/桌面回退门禁保持一致，避免旧 Host 或原生错误字符串绕过分类。
- 下一阶段继续补充真实凭据下页面直连错误、固化工具缺失和多页面恢复回归，并在平台签名密钥可用后完成安装包干净安装、覆盖升级和卸载恢复烟测。

## 最近一次专项 Review（2026-09-02 · Stage 3BX）

结论：继续演进。

- 发现动态 CAP 目录会持久化到缓存，Host 重启后旧 `searchId/toolId` 仍可能通过目录校验并触发新的计费调用；持久化目录适合诊断展示，但不应成为跨进程授权凭据。
- 现在每个 Local Host 进程生成独立目录会话标识。动态测试同时校验当前会话、最近一次 Search ID 和目录内工具；重启后旧目录仍可查看，但测试在出站前返回 `CAPABILITY_NOT_VERIFIED`，不产生额外上游调用。
- 新会话完成能力目录刷新后即可恢复测试；固定 CAP、Skill 暴露和模型工具链不受影响，会话标识不进入用户状态、备份或对外 schema。
- 下一阶段继续补充真实凭据下目录刷新失败、并发刷新和桌面重启回归，并在平台签名密钥可用后完成安装包干净安装、覆盖升级和卸载恢复烟测。

## 最近一次 Review（2026-09-01）

结论：继续演进。

- 已修复设置页无法确认真实 CAP 链路、行情刷新期间对话静默拒绝，以及 QA 脚本依赖固定演示数据的问题；这些路径现在均有明确状态、恢复动作或状态无关的断言。
- 数据边界、凭据脱敏、用户状态 CAS、CAP 固化/动态目录、费用单位隔离和 Windows 覆盖升级检查全部通过 `npm run review:architecture`。
- 本轮下一阶段验收条件：继续补充真实凭据下的 CAP/模型端到端回归，并在具备平台签名密钥后完成 Windows/macOS 干净安装、覆盖升级和卸载恢复烟测；在此之前不宣称自动更新和签名发布已完成。

## 最近一次专项 Review（2026-09-01 · Stage 3AJ）

结论：继续演进。

- 能力工作台不再仅以 HTTP 2xx 或审计成功判定测试可用；行情要求有效正数价格，其它 CAP 的空集合显示为“成功但无可展示数据”。
- 失败结果仍通过显式状态码/失败标志进入友好错误状态，详细原因只在脱敏调用日志中查看，避免空响应被误认为能力可用或网络故障。
- 下一阶段继续补充真实凭据下的动态 CAP 空/失败响应回归，并在平台签名密钥可用后完成安装包干净安装与覆盖升级烟测。

## 最近一次专项 Review（2026-09-01 · Stage 3AK）

结论：继续演进。

- 动态 CAP 测试现在允许在开发面板直接编辑受控 JSON 对象，能够覆盖目录样例未包含的周期、日期和筛选字段。
- 非法 JSON、数组和空值在网络请求前被拒绝；有效参数沿用 `search_id/tool_id` 绑定，不扩大 Host 的动态工具权限，也不进入用户状态。
- 下一阶段继续补充真实凭据下的动态参数 schema 回归，并在平台签名密钥可用后完成安装包干净安装与覆盖升级烟测。

## 最近一次专项 Review（2026-09-01 · Stage 3AL）

结论：继续演进。

- 发现并修复市场页自选表格把列对象直接传给按字符串判断的取值函数的问题；真实行情现在在顶部卡片、市场宽度和表格中保持一致。
- 修复通过前端回归覆盖价格、涨跌幅、市盈率和市净率；缺失字段仍保持空值，不改变 CAP 或缓存契约。
- 下一阶段继续补充真实凭据下多市场字段映射回归，并在平台签名密钥可用后完成安装包干净安装与覆盖升级烟测。

## 最近一次专项 Review（2026-09-01 · Stage 3AM）

结论：继续演进。

- 统一行情涨跌方向的视觉口径；缺失、非法和零涨跌幅不再被默认标为上涨或下跌，避免把不完整的真实响应误读为方向信号。
- 研究筛选只接受有效非零涨跌幅，缺失值保留在“全部”范围；价格、CAP、缓存和数据来源契约未改变。
- 下一阶段继续补充真实凭据下多市场字段映射和缺失字段回归，并在平台签名密钥可用后完成安装包干净安装与覆盖升级烟测。

## 最近一次专项 Review（2026-09-01 · Stage 3AS）

结论：继续演进。

- 研究筛选结果现在支持鼠标、Enter 和空格键进入对应个股详情，复用统一的 `selectSymbol` 状态，不新增请求或复制行情数据。
- 无效键不会触发导航，结果行保留按钮语义和可见焦点；搜索、方向筛选、行情覆盖和排序仍只作用于真实 `liveQuotes`。
- 下一阶段继续补充真实凭据下的研究筛选多市场字段回归，并在平台签名密钥可用后完成安装包干净安装与覆盖升级烟测。

## 最近一次专项 Review（2026-09-01 · Stage 3AT）

结论：继续演进。

- 事件日历卡片现在提供“查看标的”入口，列表和月视图详情共用同一事件卡渲染逻辑，直接复用 `selectSymbol` 进入详情工作区。
- 事件来源链接仍保持独立的安全外链；无证券代码的事件不显示详情入口，事件筛选和真实数据空态不受影响。
- 下一阶段继续补充真实凭据下的事件来源与跨市场代码回归，并在平台签名密钥可用后完成安装包干净安装与覆盖升级烟测。

## 最近一次专项 Review（2026-09-01 · Stage 3AU）

结论：继续演进。

- 新增 Ctrl/Cmd+K 全局命令面板，支持页面导航和标的搜索；键盘选择、Enter 执行、Escape/遮罩关闭均保持在组件内完成。
- 页面和标的操作分别复用 `setActiveView` 与 `selectSymbol`，不引入第二套状态或额外行情请求；命令目录只使用本地脱敏元数据。
- 下一阶段继续补充真实凭据下的跨市场代码检索回归，并在平台签名密钥可用后完成安装包干净安装与覆盖升级烟测。

## 最近一次专项 Review（2026-09-01 · Stage 3AX）

结论：继续演进。

- 发现结构化行情中的 `null`/空字符串会被 JavaScript 隐式转成 0，已在 Store 响应边界增加显式数值归一化；非正价格不再进入行情覆盖、方向统计或组合计算。
- 缺失涨跌幅保持空值；只有上游提供有效涨跌幅，或同时提供有效昨收与涨跌额时才计算方向字段，避免误显示平盘。
- 下一阶段继续补充真实凭据下多市场字段回归，并在平台签名密钥可用后完成安装包干净安装与覆盖升级烟测。
## 最近一次专项 Review（2026-09-02 · Stage 3AY）

结论：继续演进。

- 发现市场宽度与异动雷达会消费已超过 15 分钟的旧行情；现在只在派生的当前市场统计中排除明确过期值，原始报价仍保留并标注延迟时间。
- 过期数量与无价格数量分开统计，避免把“暂时延迟”误报为“没有数据”；时间未知的真实响应保持可见，不猜测时间。
- 下一阶段继续补充真实凭据下跨市场时间戳和交易时段回归，并在平台签名密钥可用后完成安装包干净安装与覆盖升级烟测。

## 最近一次专项 Review（2026-09-02 · Stage 3AZ）

结论：继续演进。

- 发现 Local Host 请求在代次失效后仍会继续占用连接；现在支持调用方 AbortSignal，并在数据渠道切换时取消批量行情、详情、序列和事件请求。
- 主动取消使用独立错误码，不再伪装成网络超时；代次校验仍保留，确保桌面端和无法中止的原生调用也不会把旧结果写入新配置状态。
- 下一阶段继续补充真实凭据下取消、费用审计和跨市场时间戳回归，并在平台签名密钥可用后完成安装包干净安装与覆盖升级烟测。

## 最近一次专项 Review（2026-09-02 · Stage 3BA）

结论：继续演进。

- 发现 Local Host 集成状态读取失败会被降级为浏览器预览，掩盖 Host 连接故障并诱导用户重新填写已经保存的凭据；现在错误会传播到 Store 与设置页的可恢复错误状态。
- 健康的 localhost 状态仍明确标记为 `environment: local-host`，浏览器预览和桌面原生状态路径不变；失败期间不填充示例数据。
- 下一阶段继续补充真实凭据下的 Host 恢复、跨重启状态和数据连接探针回归，并在平台签名密钥可用后完成安装包干净安装与覆盖升级烟测。

## 最近一次专项 Review（2026-09-02 · Stage 3BB）

结论：继续演进。

- 发现 Local Web Host 离线时用户状态会回退浏览器 `localStorage`，造成与 canonical Host 状态分叉；现在 localhost 严格传播 Host 读取错误，恢复后重新读取同一 revision。
- 浏览器预览仍保留独立的受限本地状态，桌面端仍走原生 Host；三种环境不会互相回写凭据、模型或用户数据。
- 下一阶段继续补充跨重启状态恢复、CAS 冲突和真实凭据下的恢复回归，并在平台签名密钥可用后完成安装包干净安装与覆盖升级烟测。

## 最近一次专项 Review（2026-09-02 · Stage 3BC）

结论：继续演进。

- 发现用户状态读取失败后仍会标记为已加载，可能让后台任务继续运行；现在保留未加载状态，显示独立的可恢复重试入口，并阻止依赖用户状态的后台流程。
- 发现慢速 Host 返回时，用户在首屏做出的编辑可能被远端快照覆盖；现在以启动默认状态为三方合并基线，保留早期编辑并继续使用 revision/CAS 防止并发覆盖。
- 下一阶段继续补充真实 Host 跨重启、恢复期间编辑和 CAS 冲突的集成回归，并在平台签名密钥可用后完成安装包干净安装与覆盖升级烟测。

## 最近一次专项 Review（2026-09-02 · Stage 3BD）

结论：继续演进。

- 发现全局命令面板可打开非自选标的，但详情页的实时行情、详情和序列动作依赖自选列表，按钮实际不会发起请求；现在请求层使用目录元数据作为临时标的上下文，仍只接受真实 CAP 返回。
- 临时研究标的不会因为查看或刷新自动写入用户状态；用户明确收藏后才进入批量刷新、研究筛选和盯盘范围，静态目录价格不参与真实数据展示。
- 下一阶段继续补充真实凭据下非自选标的跨市场参数、收藏后批量刷新和请求取消回归，并在平台签名密钥可用后完成安装包干净安装与覆盖升级烟测。

## 最近一次专项 Review（2026-09-02 · Stage 3BE）

结论：继续演进。

- 发现相同 CAP 参数被多个调用方复用时，在途请求绑定首个调用方的 `AbortSignal`；首个页面取消会误伤其它仍需要同一真实结果的页面或后台任务。
- 现在共享请求使用独立的上游控制器，每个等待者可单独取消；仅当没有任何等待者时才中止上游，避免重复计费和跨页面错误传播。
- 下一阶段继续补充真实凭据下的共享请求缓存命中、全部等待者取消和跨渠道切换回归，并在平台签名密钥可用后完成安装包干净安装与覆盖升级烟测。
## 最近一次专项 Review（2026-09-02 · Stage 3BF）

共享 CAP 请求的结果缓存已移动到共享上游 Promise 的完成路径。这样首个等待者取消时，仍存活的等待者仍可获得并提交真实结果，后续页面不会因为首个调用方离开而重复付费请求。设置、凭据、Provider 或网关变化会先递增缓存代次、清空旧值，再中止并移除所有未完成的上游请求；旧 Promise 即使迟到也无法回写新代次缓存。主动取消使用可识别的 AbortError，跳过错误 CAP 审计与重试，正常失败仍保留脱敏诊断。

## 最近一次专项 Review（2026-09-02 · Stage 3BG）

结论：继续演进。

- 发现 CAP 行情的非正价格门禁只在前端归一化层生效，Host 共享缓存和其它派生链路仍可能短暂看到 `0` 或负数。
- 现在 Host 在 `normalizeCapabilityResult("quote")` 边界拒绝非有限、缺失和非正价格；无效响应不会进入短缓存或真实数据状态，前端防御性门禁继续保留。
- 下一阶段继续补充真实凭据下多市场价格字段映射和跨重启缓存恢复回归，并在平台签名密钥可用后完成安装包干净安装与覆盖升级烟测。

## 最近一次专项 Review（2026-09-02 · Stage 3BH）

结论：继续演进。

- 发现固定 CAP 直连结果被统一标记为 `cacheHit=true`，开发面板、对话审计和详情空结果回退无法区分真实上游调用与短缓存复用。
- 现在首次直连明确标记未命中，只有进程内短缓存或共享在途结果复用才标记命中；详情多能力请求按全部子请求复用计算命中状态。
- Pi 审计会区分 `cap-call` 和 `cached-call`，避免错误命中导致额外 Search；下一阶段继续补充真实凭据下成本字段和跨重启缓存恢复回归，并在平台签名密钥可用后完成安装包干净安装与覆盖升级烟测。

## 最近一次专项 Review（2026-09-02 · Stage 3BI）

结论：继续演进。

- 发现固定 CAP 失败会无差别进入固化工具回退，认证失败、限流和服务端故障可能因此追加一次 Search/Call，增加用户等待和费用。
- 现在按错误类别门禁回退：能力失效允许回退；401/403、408/425/429/5xx、超时和网络抖动直接保持可恢复失败；交易日历始终 fail closed。
- 下一阶段继续补充真实凭据下不同 HTTP 状态和上游费用字段回归，并在平台签名密钥可用后完成安装包干净安装与覆盖升级烟测。

## 最近一次专项 Review（2026-09-02 · Stage 3BJ）

结论：继续演进。

- 发现 Stage 3BI 只约束 `/api/data/query`，模型 `foliomind_data` 捕获固定 CAP 异常后仍无条件进入固化工具回退，认证失败或暂时故障会追加 Search/Call。
- 现在 HTTP 数据路由与对话工具共享按 kind 的回退门禁：能力失效才允许回退；认证、限流、超时和上游故障直接传播，交易日历保持 fail closed；审计先记录 `cap-call` 失败，实际回退才记录 `cached-call`。
- 下一阶段继续补充真实凭据下多轮模型工具调用、成本字段和跨市场日历回归，并在平台签名密钥可用后完成安装包干净安装与覆盖升级烟测。

## 最近一次专项 Review（2026-09-02 · Stage 3BK）

结论：继续演进。

- 发现自动复盘日历门禁无视持仓市场，所有组合都固定查询上交所；现在按明确市场和可验证的 A 股代码前缀解析交易所，沪深混合组合分别核对 `212001`/`212100`，港股使用 `212200`。
- 美股、未知市场或无法从代码确定交易所的持仓会 fail closed，不发起错误的 SSE 查询；任一已支持市场休市或日历请求失败都不会生成复盘，Web 本地调试与桌面 worker 共用同一解析规则。
- 下一阶段继续补充真实凭据下跨市场日历时区、交易时段和成本回归，并在平台签名密钥可用后完成安装包干净安装与覆盖升级烟测。

## 最近一次专项 Review（2026-09-02 · Stage 3BL）

结论：继续演进。

- 发现 standalone Host 在浏览器连接断开后仍可能继续等待上游 CAP/模型响应，关闭页面不能及时回收网络资源和潜在费用。
- 现在每个 HTTP 请求拥有独立生命周期信号；客户端断开会取消所属上游请求，记录受控的 499/`client-disconnected` 诊断，并阻止关闭连接后的响应写入；共享 CAP 仍按剩余等待者决定是否继续。
- 下一阶段继续补充真实凭据下取消与成本账本、共享请求和桌面 Host 对齐回归，并在平台签名密钥可用后完成安装包干净安装与覆盖升级烟测。

## 最近一次专项 Review（2026-09-02 · Stage 3BM）

结论：继续演进。

- 发现用户状态保存失败会被 `persistSnapshot` 静默吞掉，当前操作可能显示成功，后台 fire-and-forget 写入还可能产生未处理 Promise 拒绝。
- 现在等待持久化的用户操作会传播脱敏错误；全局通知提供去重的“重试保存”，revision/CAS 冲突改为“重新读取”，调度、通知和页面异步动作均有显式异常兜底。
- 下一阶段继续补充真实 Host 跨重启、冲突恢复和费用审计回归，并在平台签名密钥可用后完成安装包干净安装与覆盖升级烟测。

## 最近一次专项 Review（2026-09-02 · Stage 3BN）

结论：继续演进。

- 发现市场页已有市场宽度，但缺少对当前自选数值字段的分布概览，用户需要逐行扫描才能判断价格、成交量和估值的整体范围。
- 现在新增字段级自选汇总，只纳入有效且未过期的真实行情，按字段独立计算最小值、平均值、中位数和最大值；缺失字段、无效价格和过期行情保持可解释空态。
- 下一阶段继续补充真实多市场字段单位、极端值格式化和大规模自选性能回归，并在平台签名密钥可用后完成安装包干净安装与覆盖升级烟测。

## 最近一次专项 Review（2026-09-02 · Stage 3BO）

结论：继续演进。

- 发现盯盘固定 CAP 的异常在查询层被吞掉，随后无条件进入模型 Search；认证失败、限流和上游暂时故障会重复等待并产生不必要费用。
- 现在保留直接 CAP 错误并按类别设置回退护栏：认证/权限、限流、超时、网络断开、Host 取消和 5xx 直接返回友好失败；只有能力缺失或结构不可用才允许一次受控回退，多条件遇到阻断错误不使用部分结果。
- 下一阶段继续补充真实凭据下不同状态码、模型成本字段和桌面/Web 盯盘语义对齐回归，并在平台签名密钥可用后完成安装包干净安装与覆盖升级烟测。

## 最近一次专项 Review（2026-09-02 · Stage 3BP）

结论：继续演进。

- 发现 Local Host 费用提取会递归读取通用 `amount`，可能把成交额、价格或序列中的业务数值误记为调用费用；桌面 CAP 解析也没有完整覆盖结构化 `cost`。
- 现在三条链路只接受显式计费字段及受限结构化金额，缓存命中不计入上游调用分母；缺失费用保持未知，不使用固定单价或余额变化推断。
- 下一阶段继续补充真实网关不同费用 envelope、单位和跨平台审计字段回归，并在平台签名密钥可用后完成安装包干净安装与覆盖升级烟测。

## 最近一次专项 Review（2026-09-02 · Stage 3BQ）

结论：继续演进。

- 发现应用启动时集成状态尚未从 Local Host/桌面返回，首页会短暂落入“预览模式”判断，用户无法区分连接初始化与浏览器演示。
- 现在 Store 初始进入连接读取态；首页行情状态、自选侧栏和价格摘要均区分连接读取、连接失败、真实行情和浏览器预览，读取期间不触发行情请求或渲染示例数字。
- 下一阶段继续补充桌面冷启动、Host 暂时不可用和真实凭据恢复场景回归，并在平台签名密钥可用后完成安装包干净安装与覆盖升级烟测。

## 最近一次专项 Review（2026-09-02 · Stage 3BR）

结论：继续演进。

- 发现组合页已有逐条盘后复盘，但缺少连续快照的表现趋势，用户需要手动展开多条记录才能判断盈亏比例变化。
- 现在新增纯前端趋势派生：只纳入有真实盈亏比例和可解析日期的复盘，同日取最新快照，少于两个有效点位保持空态，趋势图不发起新请求或写入状态。
- 下一阶段继续补充复盘跨时区日期、极端比例格式化和桌面窄屏回归，并在平台签名密钥可用后完成安装包干净安装与覆盖升级烟测。

## 最近一次专项 Review（2026-09-02 · Stage 3BT）

结论：继续演进。

- 发现 Local Web Host 保存的模型/CAP 地址没有复用桌面 Host 的 HTTPS、回环、凭据、查询参数和片段校验，恶意或误配地址可能在本地调试时接收长期 API Key。
- 现在保存、同步模型、读取配置和所有上游请求前统一执行地址安全校验：远程强制 HTTPS，仅 localhost、127.0.0.0/8 与 ::1 允许 HTTP；含凭据、query、fragment 或首尾空格的地址直接拒绝。
- 新增 Host 单测和真实 HTTP 集成测试，验证拒绝配置不会落盘或触发上游请求；桌面端原有 Rust 校验、凭据边界和 CAP 参数不变。
- 下一阶段继续补充真实凭据下地址切换、配置损坏恢复和跨平台网络错误回归，并在平台签名密钥可用后完成安装包干净安装与覆盖升级烟测。
