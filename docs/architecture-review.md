# FolioMind 架构与整体实现 Review

这是发布前和周期性质量门禁，不替代代码 Review。当前仓库提供 `npm run review:architecture` 做可重复的静态检查，检查结果只描述“是否需要关注”，不自动修改代码。

## 频率与触发

- 每个正式 Release 前运行一次，与 `npm test`、`npm run build`、安全审计和桌面端构建一起记录。
- 快速迭代期间每两周人工 Review 一次；涉及数据渠道、凭据、状态 schema、Tauri Host 或安装升级逻辑时立即追加一次专项 Review。
- 当连续两个版本出现同一类失败、数据延迟或用户无法恢复时，必须升级为重构候选项，而不是继续增加补丁。

## 人工检查清单

1. 数据边界：页面是否只消费真实 CAP 返回；缺失、失败、过期和部分数据是否分别呈现；是否存在示例数据或由其它字段推算出的数值。
2. 性能与成本：是否复用固化 CAP 工具和短时缓存；并发、超时和重试是否有上限；CAP 与模型网关费用是否在开发面板可追溯。
3. 安全与隐私：API Key、模型配置、提示词和原始响应是否保持在 Host/运行时边界内；日志、备份和错误信息是否脱敏。
4. Web/桌面一致性：状态 schema、错误恢复、取消、通知和开发面板是否共用同一语义；桌面升级是否覆盖安装而不要求先卸载。
5. 可维护性：跨页面逻辑是否已提取为纯函数；重复的 provider 适配、状态归一化和数据状态文案是否需要合并；测试是否覆盖失败和空态。
6. 发布可靠性：版本号、PRD 阶段、Release tag、安装包、校验和及升级路径是否一致；失败时能否从日志定位到接口、参数、返回摘要和原因。

Review 结论应记录在对应 PR 或 Release notes 中，至少包含“继续演进 / 优先重构 / 暂停发布”之一和下一阶段验收条件。

## 最近一次 Review（2026-09-01）

结论：继续演进。

- 已修复设置页无法确认真实 CAP 链路、行情刷新期间对话静默拒绝，以及 QA 脚本依赖固定演示数据的问题；这些路径现在均有明确状态、恢复动作或状态无关的断言。
- 数据边界、凭据脱敏、用户状态 CAS、CAP 固化/动态目录、费用单位隔离和 Windows 覆盖升级检查全部通过 `npm run review:architecture`。
- 本轮下一阶段验收条件：继续补充真实凭据下的 CAP/模型端到端回归，并在具备平台签名密钥后完成 Windows/macOS 干净安装、覆盖升级和卸载恢复烟测；在此之前不宣称自动更新和签名发布已完成。

## 最近一次专项 Review（2026-09-01 · Stage 3AJ）

结论：继续演进。

- 能力工作台不再仅以 HTTP 2xx 或审计成功判定测试可用；行情要求有效正数价格，其它 CAP 的空集合显示为“成功但无可展示数据”。
- 失败结果仍通过显式状态码/失败标志进入友好错误状态，详细原因只在脱敏调用日志中查看，避免空响应被误认为能力可用或网络故障。
- 下一阶段继续补充真实凭据下的动态 CAP 空/失败响应回归，并在平台签名密钥可用后完成安装包干净安装与覆盖升级烟测。

## 最近一次专项 Review（2026-09-01 · Stage 3AK）

结论：继续演进。

- 动态 CAP 测试现在允许在开发面板直接编辑受控 JSON 对象，能够覆盖目录样例未包含的周期、日期和筛选字段。
- 非法 JSON、数组和空值在网络请求前被拒绝；有效参数沿用 `search_id/tool_id` 绑定，不扩大 Host 的动态工具权限，也不进入用户状态。
- 下一阶段继续补充真实凭据下的动态参数 schema 回归，并在平台签名密钥可用后完成安装包干净安装与覆盖升级烟测。

## 最近一次专项 Review（2026-09-01 · Stage 3AL）

结论：继续演进。

- 发现并修复市场页自选表格把列对象直接传给按字符串判断的取值函数的问题；真实行情现在在顶部卡片、市场宽度和表格中保持一致。
- 修复通过前端回归覆盖价格、涨跌幅、市盈率和市净率；缺失字段仍保持空值，不改变 CAP 或缓存契约。
- 下一阶段继续补充真实凭据下多市场字段映射回归，并在平台签名密钥可用后完成安装包干净安装与覆盖升级烟测。

## 最近一次专项 Review（2026-09-01 · Stage 3AM）

结论：继续演进。

- 统一行情涨跌方向的视觉口径；缺失、非法和零涨跌幅不再被默认标为上涨或下跌，避免把不完整的真实响应误读为方向信号。
- 研究筛选只接受有效非零涨跌幅，缺失值保留在“全部”范围；价格、CAP、缓存和数据来源契约未改变。
- 下一阶段继续补充真实凭据下多市场字段映射和缺失字段回归，并在平台签名密钥可用后完成安装包干净安装与覆盖升级烟测。

## 最近一次专项 Review（2026-09-01 · Stage 3AS）

结论：继续演进。

- 研究筛选结果现在支持鼠标、Enter 和空格键进入对应个股详情，复用统一的 `selectSymbol` 状态，不新增请求或复制行情数据。
- 无效键不会触发导航，结果行保留按钮语义和可见焦点；搜索、方向筛选、行情覆盖和排序仍只作用于真实 `liveQuotes`。
- 下一阶段继续补充真实凭据下的研究筛选多市场字段回归，并在平台签名密钥可用后完成安装包干净安装与覆盖升级烟测。

## 最近一次专项 Review（2026-09-01 · Stage 3AT）

结论：继续演进。

- 事件日历卡片现在提供“查看标的”入口，列表和月视图详情共用同一事件卡渲染逻辑，直接复用 `selectSymbol` 进入详情工作区。
- 事件来源链接仍保持独立的安全外链；无证券代码的事件不显示详情入口，事件筛选和真实数据空态不受影响。
- 下一阶段继续补充真实凭据下的事件来源与跨市场代码回归，并在平台签名密钥可用后完成安装包干净安装与覆盖升级烟测。

## 最近一次专项 Review（2026-09-01 · Stage 3AU）

结论：继续演进。

- 新增 Ctrl/Cmd+K 全局命令面板，支持页面导航和标的搜索；键盘选择、Enter 执行、Escape/遮罩关闭均保持在组件内完成。
- 页面和标的操作分别复用 `setActiveView` 与 `selectSymbol`，不引入第二套状态或额外行情请求；命令目录只使用本地脱敏元数据。
- 下一阶段继续补充真实凭据下的跨市场代码检索回归，并在平台签名密钥可用后完成安装包干净安装与覆盖升级烟测。

## 最近一次专项 Review（2026-09-01 · Stage 3AX）

结论：继续演进。

- 发现结构化行情中的 `null`/空字符串会被 JavaScript 隐式转成 0，已在 Store 响应边界增加显式数值归一化；非正价格不再进入行情覆盖、方向统计或组合计算。
- 缺失涨跌幅保持空值；只有上游提供有效涨跌幅，或同时提供有效昨收与涨跌额时才计算方向字段，避免误显示平盘。
- 下一阶段继续补充真实凭据下多市场字段回归，并在平台签名密钥可用后完成安装包干净安装与覆盖升级烟测。
## 最近一次专项 Review（2026-09-02 · Stage 3AY）

结论：继续演进。

- 发现市场宽度与异动雷达会消费已超过 15 分钟的旧行情；现在只在派生的当前市场统计中排除明确过期值，原始报价仍保留并标注延迟时间。
- 过期数量与无价格数量分开统计，避免把“暂时延迟”误报为“没有数据”；时间未知的真实响应保持可见，不猜测时间。
- 下一阶段继续补充真实凭据下跨市场时间戳和交易时段回归，并在平台签名密钥可用后完成安装包干净安装与覆盖升级烟测。

## 最近一次专项 Review（2026-09-02 · Stage 3AZ）

结论：继续演进。

- 发现 Local Host 请求在代次失效后仍会继续占用连接；现在支持调用方 AbortSignal，并在数据渠道切换时取消批量行情、详情、序列和事件请求。
- 主动取消使用独立错误码，不再伪装成网络超时；代次校验仍保留，确保桌面端和无法中止的原生调用也不会把旧结果写入新配置状态。
- 下一阶段继续补充真实凭据下取消、费用审计和跨市场时间戳回归，并在平台签名密钥可用后完成安装包干净安装与覆盖升级烟测。

## 最近一次专项 Review（2026-09-02 · Stage 3BA）

结论：继续演进。

- 发现 Local Host 集成状态读取失败会被降级为浏览器预览，掩盖 Host 连接故障并诱导用户重新填写已经保存的凭据；现在错误会传播到 Store 与设置页的可恢复错误状态。
- 健康的 localhost 状态仍明确标记为 `environment: local-host`，浏览器预览和桌面原生状态路径不变；失败期间不填充示例数据。
- 下一阶段继续补充真实凭据下的 Host 恢复、跨重启状态和数据连接探针回归，并在平台签名密钥可用后完成安装包干净安装与覆盖升级烟测。

## 最近一次专项 Review（2026-09-02 · Stage 3BB）

结论：继续演进。

- 发现 Local Web Host 离线时用户状态会回退浏览器 `localStorage`，造成与 canonical Host 状态分叉；现在 localhost 严格传播 Host 读取错误，恢复后重新读取同一 revision。
- 浏览器预览仍保留独立的受限本地状态，桌面端仍走原生 Host；三种环境不会互相回写凭据、模型或用户数据。
- 下一阶段继续补充跨重启状态恢复、CAS 冲突和真实凭据下的恢复回归，并在平台签名密钥可用后完成安装包干净安装与覆盖升级烟测。

## 最近一次专项 Review（2026-09-02 · Stage 3BC）

结论：继续演进。

- 发现用户状态读取失败后仍会标记为已加载，可能让后台任务继续运行；现在保留未加载状态，显示独立的可恢复重试入口，并阻止依赖用户状态的后台流程。
- 发现慢速 Host 返回时，用户在首屏做出的编辑可能被远端快照覆盖；现在以启动默认状态为三方合并基线，保留早期编辑并继续使用 revision/CAS 防止并发覆盖。
- 下一阶段继续补充真实 Host 跨重启、恢复期间编辑和 CAS 冲突的集成回归，并在平台签名密钥可用后完成安装包干净安装与覆盖升级烟测。

## 最近一次专项 Review（2026-09-02 · Stage 3BD）

结论：继续演进。

- 发现全局命令面板可打开非自选标的，但详情页的实时行情、详情和序列动作依赖自选列表，按钮实际不会发起请求；现在请求层使用目录元数据作为临时标的上下文，仍只接受真实 CAP 返回。
- 临时研究标的不会因为查看或刷新自动写入用户状态；用户明确收藏后才进入批量刷新、研究筛选和盯盘范围，静态目录价格不参与真实数据展示。
- 下一阶段继续补充真实凭据下非自选标的跨市场参数、收藏后批量刷新和请求取消回归，并在平台签名密钥可用后完成安装包干净安装与覆盖升级烟测。
